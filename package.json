{
  "name": "displei",
  "version": "0.0.1",
  "description": "Easy web components",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "(cd ./src && browser-sync start --server --watch --files ./)"
  },
  "author": "CTNicholas",
  "license": "ISC",
  "dependencies": {
    "normalize": "^0.3.1"
  }
}
